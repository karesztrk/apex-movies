import Layout from '../layout/Layout';
import Search from '../search/Search';

const Main = () => {
    return (
        <Layout>
            <Search />
        </Layout>
    );
};

export default Main;
